import 'package:flutter/cupertino.dart';

class CamaraScreen extends StatelessWidget {
  const CamaraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center (child: Text('camara Screen'),);
  }
}